# kubectl create secret generic hf-secret --from-literal=HF_TOKEN=your_token
# Or apply after replacing the placeholder (do not commit real tokens):
apiVersion: v1
kind: Secret
metadata:
  name: hf-secret
type: Opaque
stringData:
  HF_TOKEN: REPLACE_ME
